class Paper {
    constructor(x,y,width,height) {
    var options = {
        isStatic: false,
        restitution: 0.3,
        friction: 0.5,
        density: 1.2
    }
    }
    
display (){
    circle = Bodies.circle(200,200,20,option);
    ellipseMode(RADIUS);
      ellipse(this.circle.position.x, this.circle.position.y, 20, 20);

}
}